const PATHS = {
  HOME: '/',
  NOT_FOUND: '/404',
  EVERYTHING_ELSE: '*',
};

const EVENT_LISTENERS = {
  TYPE: {
    POPSTATE: 'popstate',
  },
};

const ERROR_MESSAGES = {
  INVALID_PATH: 'Invalid path:',
  INVALID_COMPONENT: 'Invalid component tag name:',
};

const STRING = 'string';

const SHADOW_OPEN = 'open';

export { PATHS, EVENT_LISTENERS, ERROR_MESSAGES, STRING, SHADOW_OPEN };
